# 代码导入与修改记录

原始代码来自研究者从服务器下载的 AccessWiki_code_review_20260926_220429.zip。该检查包共 78 项，本仓库只导入正式三主题所需的两个生成器、八份语音脚本、事实表、四份来源文本和来源元数据；早期七主题预览、历史备份与 OCR 大量中间结果保留在原检查包中。

导入前 SHA256 见 imported-source-hashes.json。该文件记录原输入版本，不是当前修改后脚本的哈希。

## 证据网页程序

项目根目录改为相对于脚本的位置。来源清单从七主题预览清单整理为正式三主题的元数据；去除该清单中本包不含的预览音频/预览脚本引用。HTML 模板与文本处理保持不变。增加 --output-dir，默认生成至 generated/evidence_html。输出清单改用相对来源路径和页面文件名。

## 音频程序

项目路径改为相对于脚本的位置；外部 CosyVoice 和模型目录通过环境变量指定。默认输出改为 generated/audio。新增 --scripts-only；Torch/TorchAudio 导入延后到真正合成时。运行资源检查新增 CosyVoice 代码目录和 FFmpeg 检查。

合成内容、切句、随机种子算法、说话人提示、拼接停顿和响度参数均保持原代码。未重新合成音频，未声称修订后的程序能精确还原人工片段选择。

## 原始研究材料

所有现有 WAV 保持原样；网页重建在独立工作目录验证，没有覆盖原始 HTML。现场手动操作补充见 SESSION-NOTE.md。

历史 JSON 清单中的服务器路径替换为项目相对位置或 ${COSYVOICE_ROOT} 标记；原始清单保存在未修改的本地来源包中。标记用于说明来源，不代表模型已随仓库提供。
